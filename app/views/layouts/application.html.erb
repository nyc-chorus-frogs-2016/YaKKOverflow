<!DOCTYPE html>
<html>
<head>
  <title>YakOverflow</title>
   <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

</head>
<body>

  <div id="heading">
    <div id = "logo">
      <%= link_to image_tag("http://s15.postimg.org/x5ej5aem3/logo.jpg", size:"200x40"), root_path %>
    </div>
    <div id="navbar">
      <% if current_user %>
          Welcome <%= current_user.name %> |
           <%= link_to 'Log out', session_path, method: :delete %> | <%= link_to 'Home', questions_path %>
      <% else %>
            <%= link_to 'home', questions_path %> |
            <%= link_to 'sign up', register_path %> | Login via:
            <%= link_to image_tag("yak.png", size: "20x20"), login_path %> or
            <%= link_to image_tag("facebook.png", size: "20x20"), "auth/facebook" %> or
            <%= link_to image_tag("github.png", size: "20x20"), "auth/github" %> or
            <%= link_to image_tag("twitter.png", size: "20x20"), "auth/twitter" %>
       <% end %>
    </div>

   </div>


<% if flash.any?  %>
  <div>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "flash-#{name}" %>
    <% end %>
  </div>
<% end %>

<%= yield %>

</body>
</html>
